/** Default daily analyses granted to each new registered user. */
export const NEW_USER_DAILY_ALLOWANCE = 2;

export const NEW_USER_ALLOWANCE_NOTE =
  "New accounts start with 2 daily uses. An admin can grant more when capacity allows. Uses reset daily.";
